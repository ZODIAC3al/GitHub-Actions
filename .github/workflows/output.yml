name: Output Information
on: workflow_dispatch
jobs:
  info:
    runs-on: windows-latest
    steps:
      - name: Output GitHub Context
        run: echo "${{toJson(github)}}" # getting some metadata => as data object

